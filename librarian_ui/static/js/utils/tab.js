// Generated by CoffeeScript 1.10.0
(function(window, $) {
  return $.fn.tab = function() {
    var elem, handles, panels;
    elem = $(this);
    handles = elem.find('.tab-handles .handle');
    panels = elem.find('.tab-panels .panel');
    handles.each(function() {
      return $(this).on('click', function(e) {
        var handle, panel;
        handle = $(this);
        panel = $('#' + handle.find('a').data('panel'));
        handles.removeClass('active');
        panels.removeClass('active');
        handle.addClass('active');
        return panel.addClass('active');
      });
    });
    return handles.first().click();
  };
})(this, this.jQuery);
